export const addCounter = () => {
    return {
        type: "ADD_COUNTER"
    }
}

export const subtractCounter = () => {
    return {
        type: "SUBTRACT_COUNTER"
    }
}

export const resetCounter = () => {
    return {
        type: "RESET_COUNTER"
    }
}
